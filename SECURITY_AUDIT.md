# 安全审计报告

- **审计对象**：`zhuma-fakao-review`（竹马法考错题复习助手）
- **初次审计**：2026-09-10；**修复后复审**：2026-09-10（本版）
- **审计范围**：`SKILL.md`、`README.md`、`scripts/`（6 个 JS）、`references/`、`assets/`、`examples/`
- **审计方法**：关键词扫描（命令执行 / 隐蔽执行 / 网络请求 / 敏感路径 / 凭证 / 文件操作 / 权限提升 / 依赖安装）+ 语义判定 + **数据流分析（凭据落盘 / 数据外发通道）+ 提示注入面评估**。
  - 局限性说明：本报告由静态审查得出，未做动态渗透测试；「无恶意代码」结论仅覆盖仓库内静态内容，不覆盖运行时依赖（npm 包）的完整性。

## 执行摘要

| 等级 | 初审 | 修复后 | 结论 |
|---|---|---|---|
| 🔴 Critical/High | 0 / **1** | **0** | 明文凭据落盘已删除（见「本轮修复记录」） |
| ⚠️ Medium | 3 | 2（1 已缓解，2 已披露） | LLM 数据外发与抓取风控属技能固有属性，已如实披露 |
| **安全评分** | ~~95/100~~（虚高） | **90 / 100** | ✅ 可安全使用（已读并接受「数据流向」章节的前提下） |

## 本轮修复记录（2026-09-10）

| # | 初审发现 | 严重度 | 修复方式 |
|---|---|---|---|
| 1 | `01_login.js` 把 `storageState()`（含 256 位登录 token）明文写入 `scrape/state.json`，且无任何脚本读取，属纯冗余凭据副本；初审报告曾**错误声明**"脚本不写入凭证" | High | **已删除该写入**。登录态只存 `scrape/profile/`；新增 `.gitignore` 防止运行产物入库 |
| 2 | 三个 subagent 提示词模板无任何防注入约束（题干/解析/myNote 是远端不可信文本，且 subagent 持有文件工具） | Medium | **三个模板均已加入「数据与指令的边界」条款** + 修订员/笔记员写入路径白名单（只准写 `scrape/notes/` 指定文件） |
| 3 | README/SKILL 声称"不上传任何第三方"，但错题与学情实际经 subagent 发送给 LLM 服务商；`--desktop` 会经 OneDrive 同步上云 | Medium | **已如实披露**（README「隐私与数据流向」、SKILL 阶段 0 要求先告知用户、已知限制第 6 条） |
| 4 | 抓取节奏偏激进（~3 req/s 持续 9 分钟）、`accept-q` 校验串全程重放、失败无退避，有风控/封号风险 | Medium | **已加固**：默认 150ms 串行间隔（参数下限 100ms）、指数退避、连续失败 10 次熔断、20s fetch 超时；api_reference 补充 accept-q 重放风险说明；README 增加遵守平台条款提示 |
| 5 | 依赖无版本锁定（无 package.json/lockfile，浮动 latest） | Low | 新增 `package.json`（playwright-core 下限 1.42），文档建议安装时固定版本 |
| 6 | 初审方法学缺陷：纯关键词扫描漏掉数据流类风险，却给出 95/100 量化分 | Info | 本版方法学扩展为「特征扫描 + 数据流 + 注入面」，评分口径已修正并在正文保留局限性声明 |

## 详细检查结果（修复后状态）

### 命令执行与恶意代码

- 全仓库不含 `curl` / `wget` / `eval(` / `exec(` / `child_process` / `spawn` / `os.system` / `subprocess` / `sudo` / `chmod`；无远程脚本下载执行、无 `rm -rf`、无隐蔽输出屏蔽、无权限提升。
- 唯一触及用户数据的删除操作是 `03_build_units.js` 重建 `scrape/units/`（路径硬约束在工作目录内，改为 `fs.rmSync(UNITS)` 整目录重建，行为与初审描述一致）；另有 `env.js` 交付/运行锁文件的自清理 `unlink`，不触及用户数据。`04_render_pdf.js` 会对交付目录内本次未产出的旧 PDF 做清理（带日志）。

### 网络请求

| 目标 | 用途 | 判定 |
|---|---|---|
| `https://www.zhumavip.com/w/qrlogin` | 扫码登录 | 技能目标站点本身 |
| `https://www.zhumavip.com/w/my/errorbooks?...` | 错题本入口 | 同上 |
| `/java-api/api/...` | 抓取题目（同源相对路径，仅 3 个只读查询接口） | 同上，不交卷、不写作答记录 |
| `file:///`（经 `pathToFileURL` 构造） | 本地 HTML → PDF 渲染 | 本地，无外发 |

- 脚本网络层**外发目标仅限 `www.zhumavip.com`**，全部 URL 硬编码、无动态拼接第三方地址、无可疑 Base64。
- **工作流级数据外发（非脚本）**：阶段 4/5 的 subagent 由 LLM 驱动，错题内容与学情会发送给模型服务商；`--desktop` 的 PDF 会经 OneDrive 同步。两条通道已在 README/SKILL 显著披露，属**知情后可接受**的技能固有属性，不计为漏洞。

### 敏感路径与凭证

- `process.env.USERPROFILE` / `LOCALAPPDATA`：仅用于定位浏览器可执行文件与桌面目录，不读取这些目录下的数据。
- **无硬编码密钥 / Token**：登录态仅存 `scrape/profile/`（浏览器 userDataDir）；`state.json` 凭据副本已移除。日志只打印 token 长度，不打印内容。
- `credentials: 'include'` 是 fetch 的同源 Cookie 选项，非凭证泄露。
- 提示词模板已约束 subagent 不得读写 `scrape/profile/` 等敏感路径（纵深防御；注意该约束依赖模型遵守，非强制沙箱）。

### 提示注入面（本轮新增评估）

- 数据链：远端接口返回的题干/选项/官方解析 + 用户自写笔记 → 单元 JSON → subagent 提示词 → 笔记 → PDF。
- 缓解措施：三个模板的「数据与指令的边界」条款；审查环节要求回读原始 JSON 交叉验证；05 汇总脚本的交叉核对与"零解析即报错"机制防止被操纵的报告直接变成绿灯。
- 残余风险：若平台题库本身被投毒，注入文本仍可能影响笔记内容（LLM 防注入靠提示词约束，非硬隔离）。可能性低、影响中，接受并在文档标注。

### 文件操作

- 读写均位于 `<工作目录>/` 内；唯二例外：`--desktop`（显式传参，仅复制总册 PDF 到桌面，存在同名文件时会提示覆盖）与浏览器自身对 userDataDir 的读写。
- 所有 JSON/HTML 落盘已改为原子写（tmp+rename），中断不留截断文件；02/04 增加进程锁防止并发互相覆盖。

### 依赖

- 仅 `playwright-core` 一个运行时依赖，已通过 `package.json` 声明版本下限（≥1.42，PDF 书签所需）；文档建议 `npm install playwright-core@<固定版本>` 安装。仍建议在可信网络环境安装。

## 剩余风险（知情使用）

1. **LLM 服务商接触错题与学情数据** —— 技能核心功能所必需，无法避免，已披露。
2. **OneDrive/云同步** —— 工作目录或桌面若在同步范围内，题目数据/PDF 会上云。用户可自行选择目录规避。
3. **平台风控** —— 抓取即自动化访问，尽管已限速+退避，仍存在被风控的可能，后果由用户账号承担。
4. **subagent 路径约束非强制** —— 提示词白名单依赖模型遵守；如需硬隔离，应在无文件工具的沙箱中运行 subagent。

## 审计结论

**风险等级：低（知情使用）**。无恶意代码、无未披露的数据外发；脚本不写出任何凭据文件（登录态仅存于浏览器 profile 目录 `scrape/profile/`，该目录含会话 cookie、属敏感数据，见下方剩余风险与 README「隐私与数据流向」）。

**使用建议：✅ 可以安全使用** —— 前提是使用者已阅读 README「隐私与数据流向」章节并接受上述剩余风险。
